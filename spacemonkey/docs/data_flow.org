# Data Flow 

## Haskell <> Elm

1. Specifications for Haskell data types in `src/Modules/`
2. The modules from (1) are made in `make/Main.hs`
3. `spacemonkey/all.do` builds, installs, and runs make from (2), then copies the Elm files to `client/src/`

## Haskell <> Persistence



  
